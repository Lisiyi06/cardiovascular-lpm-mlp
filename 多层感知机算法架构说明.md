# 多层感知机（MLP）算法架构说明

## 运行环境

本项目所有 Python 脚本均需在 **`sci_computing`** conda 环境下运行。

```bash
conda activate sci_computing
```

该环境包含了 PyTorch、NumPy、SciPy、scikit-learn、matplotlib 等所需依赖。环境已导出为 `environment.yaml`（位于项目根目录），复现方式：

```bash
conda env create -f environment.yaml
```

---

## 1. 项目背景

我们有一个由 MATLAB Simulink 仿真生成的数据集，需要通过多层感知机（MLP）学习从 **6 维心脏生理指标**到 **5 维 LPM 集中参数模型参数**的逆映射。该替代模型训练完成后，可在毫秒级完成参数预测，替代昂贵的 Simulink 仿真，用于后续的物理约束优化任务。

---

## 2. 数据说明

### 2.1 数据来源

数据文件 `synthetic_dataset.mat` 由 `LHS.m`（拉丁超立方采样 + Simulink 批量仿真）生成。使用 `scipy.io.loadmat` 加载。

### 2.2 数据变量

| 变量      | 形状     | 说明                         |
| --------- | -------- | ---------------------------- |
| `X_clean` | `(N, 6)` | 6 维生理指标，dtype=float32  |
| `Y_clean` | `(N, 5)` | 5 维 LPM 参数，dtype=float32 |

### 2.3 输入特征（X）

| 列索引 | 名称  | 含义           | 单位        |
| ------ | ----- | -------------- | ----------- |
| 0      | LVEDV | 左室舒张末容积 | mL          |
| 1      | LVESV | 左室收缩末容积 | mL          |
| 2      | LVEF  | 左室射血分数   | 小数（0~1） |
| 3      | RVEDV | 右室舒张末容积 | mL          |
| 4      | RVESV | 右室收缩末容积 | mL          |
| 5      | MAP   | 平均动脉压     | mmHg        |

### 2.4 预测目标（Y）

| 列索引 | 名称   | 含义             | 单位      | 采样范围     |
| ------ | ------ | ---------------- | --------- | ------------ |
| 0      | Elvmax | 左室最大弹性     | mmHg/mL   | [1.5, 4.0]   |
| 1      | Ervmax | 右室最大弹性     | mmHg/mL   | [0.6, 2.0]   |
| 2      | R_sar  | 体循环小动脉阻力 | mmHg·s/mL | [0.5, 2.5]   |
| 3      | C_ao   | 主动脉顺应性     | mL/mmHg   | [0.04, 0.15] |
| 4      | TBV    | 总血容量         | mL        | [700, 1200]  |

---

## 3. 算法架构：MLP 回归器

### 3.1 网络结构

采用**漏斗形**（宽→窄→窄）全连接网络：

```
输入层 (6)
    ↓ Linear(6 → 128)
    ↓ BatchNorm1d(128)
    ↓ ReLU
    ↓ Dropout(p=0.2)
隐藏层 1 (128)
    ↓ Linear(128 → 64)
    ↓ BatchNorm1d(64)
    ↓ ReLU
    ↓ Dropout(p=0.2)
隐藏层 2 (64)
    ↓ Linear(64 → 32)
    ↓ BatchNorm1d(32)
    ↓ ReLU
    ↓ Dropout(p=0.2)
隐藏层 3 (32)
    ↓ Linear(32 → 5)
    ↓ （无激活函数）
输出层 (5)
```

### 3.2 各组件作用

- **BatchNorm1d**：每一层对输出做标准化（减均值、除方差），加速收敛、缓解梯度消失、允许更大学习率。
- **Dropout(p=0.2)**：训练时随机丢弃 20% 的神经元输出，迫使网络学习冗余特征，有效防止过拟合。
- **ReLU**：非线性激活函数，计算简单且不存在梯度饱和问题。
- **输出层无激活函数**：回归任务需要预测任意连续实数值，因此输出层为纯线性变换。

### 3.3 模型参数量

以 `[128, 64, 32]` 的隐藏层配置，总参数量约为 **14,981**：

```
6×128 + 128 + 128×64 + 64 + 64×32 + 32 + 32×5 + 5 = 14,981
```

模型轻量，CPU 推理单样本耗时 < 1ms。

---

## 4. 训练流程

### 4.1 数据预处理

1. **加载**：`scipy.io.loadmat` 读入 `X_clean` 和 `Y_clean`
2. **标准化**：使用 `sklearn.preprocessing.StandardScaler` 分别对 X 和 Y 做 Z-Score 标准化（减均值 ÷ 标准差），消除量纲影响
3. **划分**：`train_test_split(test_size=0.2, random_state=42)`，80% 训练 / 20% 验证
4. **封装**：`TensorDataset` + `DataLoader`，batch_size=32，训练集 shuffle

### 4.2 训练配置

| 超参数        | 值        | 说明                                 |
| ------------- | --------- | ------------------------------------ |
| 优化器        | Adam      | 自适应学习率，适合中等规模数据       |
| 学习率        | 1×10⁻³    | 初始学习率                           |
| 权重衰减      | 1×10⁻⁵    | L2 正则化，抑制过拟合                |
| 损失函数      | MSELoss   | 均方误差，回归任务的标准损失         |
| 最大 Epoch    | 500       | 上限保护                             |
| 早停 Patience | 50        | 验证损失连续 50 epoch 未创新低则停止 |
| 训练/验证比   | 80% / 20% | —                                    |

### 4.3 早停机制

每个 epoch 结束后计算验证集损失。若当前验证损失 < 历史最佳，更新最佳权重并重置计数器；否则计数器 +1。计数器达到 patience=50 时停止训练，并恢复到历史最佳权重。此举可有效防止过拟合。

### 4.4 学习率与权重衰减的关系

- **学习率决定收敛速度**：1×10⁻³ 是 Adam 优化器的推荐默认值，在本任务中表现良好。
- **权重衰减决定正则化强度**：1×10⁻⁵ 为轻度的 L2 惩罚，在保持模型表达能力的同时略微抑制过拟合。若验证集 R² 出现严重下降，可尝试调大至 1×10⁻⁴。

---

## 5. 评估指标

在验证集上反归一化后，对每个输出参数计算：

| 指标 | 公式                       | 说明                             |
| ---- | -------------------------- | -------------------------------- |
| MAE  | mean(\|true - pred\|)      | 平均绝对误差，直观反映预测偏差   |
| RMSE | sqrt(mean((true - pred)²)) | 均方根误差，对大误差敏感         |
| R²   | 1 - SS_res / SS_tot        | 决定系数，衡量模型解释方差的比例 |

R² 越接近 1 越好，MAE/RMSE 越小越好。不同参数的误差水平差异较大（如 TBV ~ 数百 mL，C_ao ~ 0.01 mL/mmHg），建议优先关注 R²。

---

## 6. 输出产物

| 文件                   | 内容                                         | 用途                       |
| ---------------------- | -------------------------------------------- | -------------------------- |
| `best_lpm_model.pt`    | 模型 state_dict + scaler_X + scaler_Y        | 推理时加载（`torch.load`） |
| `loss_curve.png`       | 训练/验证损失曲线（对数纵轴）                | 判断是否过拟合 / 欠拟合    |
| `test_predictions.npz` | true_values + predicted_values + param_names | 后续物理论证与可视化       |

---

## 7. 代码结构

```
model_train.py
├── CFG 配置字典              # 集中管理所有超参数
├── MLPRegressor 类           # 网络结构定义（漏斗形 MLP）
├── load_data()               # 数据加载（.mat / 内置测试数据）
├── prepare_dataloaders()      # 标准化 + 划分 + DataLoader
├── train_model()              # 训练循环 + 早停
├── evaluate_model()           # 反归一化评估
├── print_metrics()            # 打印 MAE / RMSE / R²
├── plot_loss_curve()          # 损失曲线绘图
├── save_artifacts()           # 保存模型 + 预测结果
└── main()                     # 主流程（argparse 支持命令行覆盖）
```

支持两种运行模式：

```bash
conda activate sci_computing

# 完整模式：使用 synthetic_dataset.mat 训练
python model_train.py

# 测试模式：使用 5 组内置数据快速验证
python model_train.py --mode test
```

---

## 8. 运行环境与依赖

### Python 环境

通过 conda 管理：

```bash
conda activate sci_computing
```

核心依赖：

| 包            | 用途                                |
| ------------- | ----------------------------------- |
| PyTorch ≥ 2.0 | 深度学习框架（CPU 即可，CUDA 可选） |
| NumPy         | 数据处理                            |
| SciPy         | 读取 .mat 文件                      |
| scikit-learn  | 标准化、数据集划分                  |
| Matplotlib    | 损失曲线绘图                        |

### MATLAB 环境

- MATLAB + Simulink
